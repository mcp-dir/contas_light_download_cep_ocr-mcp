# Ferramentas

Light: Download via CEP + OCR expõe 1 ferramenta (todas somente leitura).

### 1. `contas_light_download_cep_ocr_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `cep`, `instalacao` (opcional)

Light: Download via CEP + OCR, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Light: Download via CEP + OCR
```
